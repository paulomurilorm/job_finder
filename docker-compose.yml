version: "3.8"

services:
  job-finder-app:
    build: .
    container_name: job-finder
    ports:
      - "3000:3000"
    volumes:
      - ./backend/src/config/:/app/backend/src/config/
    restart: unless-stopped
